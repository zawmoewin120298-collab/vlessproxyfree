body {
  background-color: #f0f4f8;
  font-family: sans-serif;
  display: flex;
  justify-content: center;
  padding: 20px;
}

.vpn-card {
  background-color: white;
  border-radius: 15px;
  width: 350px;
  padding: 20px;
  box-shadow: 0 4px 6px rgba(0,0,0,0.05);
  border: 1px solid #eef2f6;
}

.card-header {
  display: flex;
  align-items: center;
  margin-bottom: 15px;
}

.flag {
  width: 50px;
  height: auto;
  border-radius: 4px;
  margin-right: 15px;
}

.vpn-name {
  color: #1a73e8;
  font-size: 1.2rem;
  margin: 0;
  font-weight: bold;
  line-height: 1.2;
}

.divider {
  border: 0;
  border-top: 1px solid #f0f0f0;
  margin: 15px 0;
}

.card-footer {
  display: flex;
  justify-content: space-between;
  color: #5f6368;
  font-size: 0.9rem;
  font-weight: 500;
}

.ping {
  color: #2ea44f; /* Green color for low ping */
  font-weight: bold;
}

